import ThemePreview from '@/app/(protected)/presentation/[presentationId]/select-theme/_components/ThemePreview';

type Props = {};

const SelectThemePage = (props: Props) => {
  return <ThemePreview />;
};

export default SelectThemePage;
